// ============================================================================
// ФАЙЛ SAMPLINGENGINE.H - ОБЪЯВЛЕНИЕ ВЫЧИСЛИТЕЛЬНОГО ЯДРА LAB_2
// ============================================================================
// Назначение файла:
// 1) объявить внешний интерфейс вычислительного ядра лабораторной работы;
// 2) отделить запуск моделирования от GUI-слоя;
// 3) зафиксировать единственную точку входа в вычислительный сценарий.
//
// По смыслу этот заголовок очень маленький, но важный:
// пользовательский интерфейс не должен знать детали реализации алгоритма,
// ему достаточно передать параметры SamplingOptions и получить
// готовый SamplingResult.
// ============================================================================

// Защита от повторного включения заголовочного файла.
#pragma once

// Подключаем общие типы параметров и результата моделирования.
#include "SamplingTypes.h"

// ----------------------------------------------------------------------------
// ФУНКЦИЯ RunSampling
// ----------------------------------------------------------------------------
// Назначение:
// Выполнить полный сценарий моделирования коррелированных случайных величин
// по переданным пользовательским параметрам.
//
// Что делает функция на верхнем уровне:
// - строит теоретические данные варианта;
// - генерирует независимую нормальную выборку U;
// - преобразует ее в выборку X;
// - вычисляет выборочные оценки;
// - подготавливает все структуры результата для GUI.
//
// Принимает:
// - options : параметры эксперимента, введенные пользователем.
//
// Возвращает:
// - SamplingResult, содержащий либо успешный полный результат моделирования,
//   либо информацию об ошибке.
// ----------------------------------------------------------------------------
SamplingResult RunSampling(const SamplingOptions& options);
